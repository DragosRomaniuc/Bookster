import rgba from "./rgba";
import {
  getMargins,
  getPaddings,
  mergeTheme,
  parseSpacing,
  spacing,
} from "./helpers";

const getSpacing = spacing;

export { rgba, getMargins, getPaddings, getSpacing, parseSpacing, mergeTheme };
export { default } from "./ui";
