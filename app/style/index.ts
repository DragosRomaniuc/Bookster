import theme, { colors, fonts, weights, sizes } from "./theme";
import ComponentsStyle from "./components";
import { layout } from "./layout";

export { colors, fonts, weights, sizes, ComponentsStyle, layout };
export default theme;
